<UserControl x:Class="EasySave.WPF.Views.AppSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EasySave.WPF.Views"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="#EEEEEE"
                                    BorderBrush="#CCCCCC"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border x:Name="CheckMarkBorder"
                                            Width="15"
                                            Height="15"
                                            Background="#CCCCCC"
                                            BorderBrush="#666666"
                                            BorderThickness="1"
                                            CornerRadius="2"
                                            Margin="0,0,5,0">
                                        <Path x:Name="CheckMark"
                                              Visibility="Collapsed"
                                              Data="M 0 5 L 5 10 L 15 0"
                                              Stroke="White"
                                              StrokeThickness="2"
                                              SnapsToDevicePixels="True"/>
                                    </Border>
                                    <ContentPresenter Grid.Column="1"
                                                      Margin="5,0,0,0"
                                                      VerticalAlignment="Center"
                                                      RecognizesAccessKey="True"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#DDDDDD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#BBBBBB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Ellipse x:Name="Border"
                             Grid.Column="0"
                             Fill="Transparent"
                             Stroke="#007ACC"
                             StrokeThickness="2"
                             Width="20"
                             Height="20"/>
                            <Ellipse x:Name="InnerCircle"
                             Grid.Column="0"
                             Fill="#007ACC"
                             StrokeThickness="0"
                             Width="12"
                             Height="12"
                             Margin="4"
                             Visibility="Collapsed"/>
                            <ContentPresenter Grid.Column="1"
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="InnerCircle" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Stroke" Value="#00568B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <local:LanguageToBooleanConverter x:Key="LanguageConverter" />

    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Backup Jobs" FontWeight="Bold" Margin="0,0,0,5"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding AppSettings.DataFilesLocation.BackupJobsFolderPath, Mode=TwoWay}" x:Name="BackupJobsFolderPathTextBox" Width="200"/>
                <Button Content="..." Click="BrowseBackupJobsFolder_Click" Style="{StaticResource ButtonStyle}" Width="30" Margin="5,0"/>
                <TextBox Text="{Binding AppSettings.DataFilesLocation.BackupJobsJsonFileName, Mode=TwoWay}" x:Name="BackupJobsJsonFileNameTextBox" Width="150"/>
            </StackPanel>

            <TextBlock Text="States" FontWeight="Bold" Margin="0,10,0,5"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding AppSettings.DataFilesLocation.StatesFolderPath, Mode=TwoWay}" x:Name="StatesFolderPathTextBox" Width="200"/>
                <Button Content="..." Click="BrowseStatesFolder_Click" Style="{StaticResource ButtonStyle}" Width="30" Margin="5,0"/>
                <TextBox Text="{Binding AppSettings.DataFilesLocation.StatesJsonFileName, Mode=TwoWay}" x:Name="StatesJsonFileNameTextBox" Width="150"/>
            </StackPanel>

            <TextBlock Text="Logs" FontWeight="Bold" Margin="0,10,0,5"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding AppSettings.DataFilesLocation.LogsFolderPath, Mode=TwoWay}" x:Name="LogsFolderPathTextBox" Width="200"/>
                <Button Content="..." Click="BrowseLogsFolder_Click" Style="{StaticResource ButtonStyle}" Width="30" Margin="5,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,5" HorizontalAlignment="left" Width="96">
                <TextBlock Text="Current Culture" FontWeight="Bold" Margin="0,0,0,5"/>
                <RadioButton Content="French" IsChecked="{Binding AppSettings.Localization.CurrentCulture, Converter={StaticResource LanguageConverter}, ConverterParameter=fr-FR}" Click="RadioButton_Click" Style="{StaticResource RadioButtonStyle}"/>
                <RadioButton Content="English" IsChecked="{Binding AppSettings.Localization.CurrentCulture, Converter={StaticResource LanguageConverter}, ConverterParameter=en-EN}" Click="RadioButton_Click" Style="{StaticResource RadioButtonStyle}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,5" HorizontalAlignment="left" Width="145">
                <TextBlock Text="Backup jobs file type" FontWeight="Bold" Margin="0,0,0,5"/>
                <RadioButton Content="json" IsChecked="{Binding AppSettings.DataFilesTypes.BackupJobsFileType, Converter={StaticResource LanguageConverter}, ConverterParameter=json}" Click="RadioButton_Click" Style="{StaticResource RadioButtonStyle}"/>
                <RadioButton Content="xml" IsChecked="{Binding AppSettings.DataFilesTypes.BackupJobsFileType, Converter={StaticResource LanguageConverter}, ConverterParameter=xml}" Click="RadioButton_Click" Style="{StaticResource RadioButtonStyle}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,5" HorizontalAlignment="left" Width="145">
                <TextBlock Text="Logs file type" FontWeight="Bold" Margin="0,0,0,5"/>
                <RadioButton x:Name="LogsFileTypeJsonRadioButton" Content="json" IsChecked="{Binding AppSettings.DataFilesTypes.LogsFileType, Converter={StaticResource LanguageConverter}, ConverterParameter=json}" Click="RadioButton_Click" Style="{StaticResource RadioButtonStyle}"/>
                <RadioButton x:Name="LogsFileTypeXmlRadioButton" Content="xml" IsChecked="{Binding AppSettings.DataFilesTypes.LogsFileType, Converter={StaticResource LanguageConverter}, ConverterParameter=xml}" Click="RadioButton_Click" Style="{StaticResource RadioButtonStyle}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,5" HorizontalAlignment="left" Width="145">
                <TextBlock Text="States file type" FontWeight="Bold" Margin="0,0,0,5"/>
                <RadioButton x:Name="StatesFileTypeJsonRadioButton" Content="json" IsChecked="{Binding AppSettings.DataFilesTypes.StatesFileType, Converter={StaticResource LanguageConverter}, ConverterParameter=json}" Click="RadioButton_Click" Style="{StaticResource RadioButtonStyle}"/>
                <RadioButton x:Name="StatesFileTypeXmlRadioButton" Content="xml" IsChecked="{Binding AppSettings.DataFilesTypes.StatesFileType, Converter={StaticResource LanguageConverter}, ConverterParameter=xml}" Click="RadioButton_Click" Style="{StaticResource RadioButtonStyle}"/>
            </StackPanel>



            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Save" Margin="5" Command="{Binding SaveAppSettingsCommand}" Style="{StaticResource ButtonStyle}" Width="56"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
    
    
