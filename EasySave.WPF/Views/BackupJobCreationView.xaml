<UserControl x:Class="EasySave.WPF.Views.BackupJobCreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EasySave.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding BackupJob.Name, Mode=TwoWay}"/>
        <TextBox Grid.Row="1" Text="{Binding BackupJob.SourceDirectory, Mode=TwoWay}"/>
        <TextBox Grid.Row="2" Text="{Binding BackupJob.DestinationDirectory, Mode=TwoWay}"/>
        <TextBox Grid.Row="3" Text="{Binding BackupJob.BackupType, Mode=TwoWay}"/>
        <Button Grid.Row="4" Command="{Binding CreateBackupJobCommand}" Content="Valider" />
    </Grid>
</UserControl>
